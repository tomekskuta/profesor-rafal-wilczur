import { App } from '@slack/bolt';

export const yourMessageFeature = (app: App): void => {
  // TODO: Create your message feature
  // app.message('some text', async ({ say }) => {
  //   await say('Say something');
  // });
};
